// main.js - simple glue to make client scripts run if not using modules
// If your environment doesn't support ES modules for static files, copy websocket functions into this file.
// This script intentionally small because canvas.js imports websocket.js in the modular version.
// For a quick run: ensure <script type="module"> is used in index.html for these scripts.
